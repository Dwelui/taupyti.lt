# HTTP

## http primitives

- Primitives such as `protocol version`, `methods`, `status codes`, etc... should be in http.h and http.c
- [ ] Move enums and implementation of these primitives to http.h and http.c
